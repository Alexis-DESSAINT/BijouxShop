@*
    Fichier : Views/Shared/_Layout.cshtml
    Projet : BijouxShop
    Rôle : layout global de toutes les vues Razor
*@

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - BijouxShop</title>

    <!-- Bootstrap (CDN pour rapidité) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Style custom -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>

<body class="bg-light">
    <!-- Barre de navigation -->
    <header class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-uppercase" asp-controller="Home" asp-action="Index">
                BijouxShop
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarMain">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Products" asp-action="Index">Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Home" asp-action="Privacy">À propos</a>
                    </li>
                </ul>

                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link position-relative" asp-controller="Cart" asp-action="Index">
                            🛒 Panier
                            @* Si tu veux afficher le nombre d’articles :
                            <span class="badge bg-primary position-absolute top-0 start-100 translate-middle">
                                2
                            </span> *@
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </header>

    <!-- Contenu principal -->
    <main role="main" class="container py-4">
        @RenderBody()
    </main>

    <!-- Pied de page -->
    <footer class="border-top mt-auto py-3 bg-white">
        <div class="container text-center text-muted small">
            &copy; @DateTime.Now.Year - <strong>BijouxShop</strong> |
            <a asp-controller="Home" asp-action="Privacy" class="text-decoration-none">Confidentialité</a>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @RenderSection("Scripts", required: false)
</body>
</html>
``
